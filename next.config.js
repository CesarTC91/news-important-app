/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.reed.co.uk', 'static.vecteezy.com', 's.hdnux.com']
  }
}

module.exports = nextConfig
